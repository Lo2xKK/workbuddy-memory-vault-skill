# 统一行尾为 LF，避免 Windows/Unix 混用造成 CRLF 噪音
* text=auto eol=lf

# 生成的对话数据（机器产物）也强制 LF
*.md text eol=lf
*.json text eol=lf
*.jsonl text eol=lf
*.py text eol=lf

# 二进制文件不做转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
